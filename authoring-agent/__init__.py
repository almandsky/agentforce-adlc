"""ADLC Authoring Agent — a Claude Agent SDK harness for the Agentforce ADLC.

Wraps the adlc-* skills and subagents in a standalone agent that can be run
via CLI or FastAPI server using the `harness` package (create-agent-sdk-app).
"""
